# demo credential only. this file is replaced by a SealedSecret before the repo goes public,
# so that the plaintext password never sits in git.
apiVersion: v1
kind: Secret
metadata:
  name: uptime-db
type: Opaque
stringData:
  POSTGRES_PASSWORD: uptime
